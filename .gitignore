node_modules/
dist/
# Standalone binaries (66MB each; distribution is the Release's job)
adapters/official/*/bin/
coverage/
# broker (Rust) build output (distribution is the Release's job; for the local cargo build fallback)
broker/target/

.env*
*.log

.paa/
credentials.json

# binaries the Quickstart downloads into the repo root (never commit a 70MB binary)
/openroly
/openroly-broker
/openroly-mcp
/paa
/paa-darwin-*
/paa-linux-*
/atn
/atn-broker
/atn-mcp
